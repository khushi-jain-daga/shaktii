# Client
VITE_API_URL=http://localhost:4000/api
VITE_WIDRSX_API_URL=http://localhost:5000

# Node API
PORT=4000
CLIENT_URL=http://localhost:5173

# Database (next integration step)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shaktii

# Authentication (replace in local .env; never commit real secrets)
JWT_SECRET=replace-with-a-long-random-secret
JWT_REFRESH_SECRET=replace-with-another-long-random-secret

# File security
ENCRYPTION_KEY=replace-with-a-32-byte-key-material

# Optional blockchain provider
BLOCKCHAIN_RPC_URL=
BLOCKCHAIN_PRIVATE_KEY=
