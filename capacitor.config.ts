import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.shaktii.pkap',
  appName: 'SHAKTII',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
